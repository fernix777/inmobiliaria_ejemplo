// Ejemplo de script de pruebas para Supabase
// ATENCIÓN: este script contenía una función que eliminaba todas las filas de
// la tabla `properties`. No se debe ejecutar en entornos con datos reales.

// Para usarlo localmente: copia este archivo a `js/testSupabase.js`, revisa y
// modifica la lógica para que se adapte a tu entorno, y elimina la llamada
// automática a `deleteAllProperties()` antes de ejecutarlo.

/*
const { createClient } = supabase;
const supabaseClient = createClient(
  'https://TU_PROYECTO.supabase.co',
  'TU_ANON_KEY'
);

async function deleteAllProperties() {
  // Implementación de ejemplo: primero confirma manualmente que deseas proceder
}

// DOCUMENTACIÓN: No llames a deleteAllProperties() automáticamente.
*/
